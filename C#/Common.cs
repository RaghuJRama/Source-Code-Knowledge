/*
        /// <summary>
        /// Holds class name
        /// </summary>
        private const string ClassName = "Common";



            // Method signature
            const string MethodSignature = "";
            // Arguments message
            string ArgumentsMessage = string.Format("Args (: {0})", );


            try
            {
            }
            catch (Exception ex)
            {
                // Error message
                string strErrorMessage = "";

                // Logs the exception message
                Log.LogExceptionToFile(MethodSignature, ClassName, this.ToString(), ArgumentsMessage, strErrorMessage, ex);

		        throw ex;
            }





            // Method signature
            const string MethodSignature = "";
            // Arguments message
            string ArgumentsMessage = string.Format("Args (: {0})", );


            try
            {
            }
            catch (Exception ex)
            {
                // Error message
                string strErrorMessage = "";

                // Logs the exception message
                Log.LogExceptionToFile(MethodSignature, ClassName, ArgumentsMessage, strErrorMessage, ex);

		        throw ex;
            }





            // Method signature
            const string MethodSignature = "";


            try
            {
            }
            catch (Exception ex)
            {
                // Error message
                string strErrorMessage = "";

                // Logs the exception message
                Log.LogExceptionToFile(MethodSignature, ClassName, this.ToString(), strErrorMessage, ex);

		        throw ex;
            }





            // Method signature
            const string MethodSignature = "";


            try
            {
            }
            catch (Exception ex)
            {
                // Error message
                string strErrorMessage = "";

                // Logs the exception message
                Log.LogExceptionToFile(MethodSignature, ClassName, strErrorMessage, ex);

		        throw ex;
            }





            // Method signature
            const string MethodSignature = "";

	        // Arguments message
            StringBuilder sbArgs = new StringBuilder();

            sbArgs.AppendFormat("Args (: {0}, ", );
            sbArgs.AppendFormat(": {0}, ", );
            sbArgs.AppendFormat(": {0})", );

            string ArgumentsMessage = sbArgs.ToString();


            try
            {
            }
            catch (Exception ex)
            {
                // Error message
                string strErrorMessage = "";

                // Logs the exception message
                Log.LogExceptionToFile(MethodSignature, ClassName, this.ToString(), ArgumentsMessage, strErrorMessage, ex);

		        throw ex;
            }






 */
